import { plutusV2 } from '../libs/plutus-v2'
import { plutusV3 } from '../libs/plutus-v3'

type Props = {
    version: string
    title: string
}

const readValidator = function ({}: Props) {
    try {
    } catch (error) {
        console.log(error)
    }
}

export default readValidator
